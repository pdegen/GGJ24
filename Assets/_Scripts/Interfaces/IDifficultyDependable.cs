namespace GGJ24
{
    public interface IDifficultydDependable
    {
        // Interface for objects that have params that depend on start on difficulty setting
        public void RefreshDifficultyParams();
    }
}